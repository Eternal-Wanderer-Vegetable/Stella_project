# Stella Migration Report Template

> Generated by `python -m deploy migrate` and normally written to
> `STELLA_HOME/migration_report.md`. Record only facts such as "reused" or
> "removed" for sensitive settings; never include API keys, tokens, passwords,
> or complete connection strings.

## Basic Information

| Item | Value |
|---|---|
| Mode | Dry run / Executed / Failed |
| Source directory | `<old installation>` |
| Source version | `<old version or unknown>` |
| Target directory | `<current program directory>` |
| Target version | `4.0.0` |
| Generated at | `<UTC timestamp>` |

## Files and Data

| Item | Result | Notes |
|---|---|---|
| `.env` | `<reused/merged/failed>` | `<counts only; no secret values>` |
| `memory/agent_memory.db` | `<imported/skipped/failed>` | `<migration summary>` |
| `data/plugins/` | `<imported/skipped/failed>` | `<file count>` |
| `config/spaces/` | `<imported/skipped/failed>` | `<conflict handling>` |
| `system_prompts/` | `<imported/skipped/failed>` | `<user changes take priority>` |
| `runtime/` | `<reused/prepared again/failed>` | `<version and checksum summary>` |

## Database Migration

- Version: `<from> -> <to>`
- Row and index checks: `<passed/failed>`
- Summary: `<no message contents or sensitive values>`

## Safety and Rollback

- The old directory remains read-only throughout the operation.
- A failure does not overwrite user data and can be retried after remediation.
- Configuration merging changes only the target directory; unrecognized settings
  are preserved or listed according to the migration rules.
- Runtime, component, and model packages are checksum-verified; a failed import
  does not switch the active version.

## User Action Required

- `<configuration conflict or manual confirmation>`
- `<NapCat/OneBot manual QR login or network configuration>`

# Python cache
__pycache__/
*.pyc

# Virtual environment
.venv/
.vscode/

# Deprecated code (all legacy/obsolete files consolidated here)
/_deprecated/

# FlexiWeb (external tool, cloned separately)
FlexiWeb_Stream_Scraper/

# Coverage data files
.coverage
.coverage-*

# Runtime data & logs
# 所有运行期日志统一落在 logs/（见 config/settings.py 的 LOG_DIR），由下面的 logs/ 一条覆盖：
#   logs/stella.jsonl              结构化日志（GUI 消费，10MB 轮转、保留 5 份）
#   logs/stella_thought_logs.md    思考/决策日志
#   logs/memory_consolidation_log.md 整合日志
#   logs/memory_compressor_log.md  压缩日志
#   logs/boot_debug.log            启动诊断
#   logs/stella.pid                进程 PID（不是日志，但同目录）
# 2026-08-25 之前这些散在项目根目录，各占一条 ignore；迁移后不再需要逐个列。
logs/
memory/agent_memory.db
memory/stella_memory_backup.db
memory/.last_message_cleanup
memory/agent_memory.db-journal
# schema 迁移前的时间戳备份（memory/schema.py:backup_snapshot）。整库副本，是用户数据。
memory/*.bak
test_compressor_trigger.py

# Benchmark 采样输出（含真实群消息数据，禁止入库）
windows_raw.json
windows_probe_positive.json

# 整合探针输出（含真实群聊内容，禁止入库）
consolidation_probe.md
consolidation_probe*.json
dump_windows.py
windows_review.txt

# 用例脱敏映射表（真实 ID → 占位符，含敏感映射关系，禁止入库）
sanitize_map.json

# Bot plugin — runtime data
/stella_project/plugins/bot_main/__pycache__/

# Environment
.env
.env.dev
.env.prod
!.env.example
.env.backup

# deploy init 的应答文件（含群号等个人配置）与 --force 的备份
deploy.answers.toml
.env.bak

# 结构化 JSON 日志（GUI 消费）与进程 PID 文件 —— 已合并到顶部的 logs/ 一条

# 停止请求哨兵（deploy stop 写入，Bot 启动时清除）及其 .tmp 临时名
.stella-stop-request*

# Misc
STARTCOMMAND.md

# 临时文件
positive_reordered.json
prompt_now.txt

# start.bat 首次运行下载的嵌入式 Python 运行时
runtime/

/memory/.space_assignments.json
/release_backup/
/config/spaces/

# AstrBot 兼容层：第三方插件、其配置与运行期数据（禁止入库）
/data/plugins/*
!/data/plugins/.gitkeep
/data/config/
/data/plugin_data/
# HTML → 图片的渲染产物（要发给 QQ 的图片，不是日志，所以不放 logs/）
/data/render_cache/

start_claude_command.txt
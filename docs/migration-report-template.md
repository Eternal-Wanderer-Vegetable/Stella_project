# Stella 配置导入报告（模板）

> 该报告由 `python -m deploy migrate` 生成，实际报告应写入当前
> `STELLA_HOME/migration_report.md`。敏感值只记录“已沿用/已移除”等事实，
> 不记录 API key、token、密码或完整连接字符串。

## 基本信息

| 项目 | 内容 |
|---|---|
| 模式 | 预演 / 已执行 / 失败 |
| 来源目录 | `<旧安装目录>` |
| 来源版本 | `<旧版本或 unknown>` |
| 目标目录 | `<当前程序目录>` |
| 目标版本 | `4.0.0` |
| 生成时间 | `<UTC 时间>` |

## 文件与数据

| 项目 | 结果 | 说明 |
|---|---|---|
| `.env` | `<沿用/合并/失败>` | `<配置项数量；敏感值不打印>` |
| `memory/agent_memory.db` | `<导入/跳过/失败>` | `<校验或迁移摘要>` |
| `data/plugins/` | `<导入/跳过/失败>` | `<文件数量>` |
| `config/spaces/` | `<导入/跳过/失败>` | `<冲突处理>` |
| `system_prompts/` | `<导入/跳过/失败>` | `<用户修改优先>` |
| `runtime/` | `<复用/重新准备/失败>` | `<版本和 checksum 摘要>` |

## 数据库迁移

- 版本：`<from> → <to>`
- 行数与索引校验：`<通过/失败>`
- 迁移摘要：`<不包含消息原文和敏感值>`

## 安全与回滚

- 旧目录在整个过程中保持只读。
- 失败时不覆盖用户数据，修复问题后可重试。
- 配置合并只修改目标目录；未识别配置按工具规则保留或列出。
- Runtime、组件和模型包的安装使用 checksum 校验；失败不切换 active 版本。

## 需要用户处理

- `<冲突文件或需要人工确认的配置>`
- `<NapCat/OneBot 仍需人工扫码或网络配置的事项>`
